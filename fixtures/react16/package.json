{
	"name": "opshot-react16",
	"version": "0.0.0",
	"private": true,
	"description": "React 16.8.0 held as its own package so npm nests it beneath the root React 19, giving the version-matrix suite a real React 16.8.0 instance to render against",
	"dependencies": {
		"react": "16.8.0",
		"react-dom": "16.8.0"
	}
}
