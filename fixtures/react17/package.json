{
	"name": "opshot-react17",
	"version": "0.0.0",
	"private": true,
	"description": "React 17.0.2 held as its own package so npm nests it beneath the root React 19, giving the version-matrix suite a real React 17 instance to render against",
	"dependencies": {
		"react": "17.0.2",
		"react-dom": "17.0.2"
	}
}
